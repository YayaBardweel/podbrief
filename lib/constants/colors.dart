import 'package:flutter/material.dart';

const Color kTextColor = Colors.black;
const Color kBackgroundColor = Colors.white;
const Color kPrimaryColor = Color(0xFF5E35B1); // Deep Purple
const Color kAccentColor = Color(0xFFB39DDB); // Soft Lavender (or use a blue-grey like Color(0xFFB0BEC5))
const Color kBackgroundColorStart = Color(0xFFEDE7F6); // Light purple hue
const Color kBackgroundColorEnd = Colors.white; // White for gradient end
const Color kTextColorDark = Color(0xFF212121); // Dark grey for general text
const Color kTextColorLight =
    Colors.white; // White for text on dark backgrounds
